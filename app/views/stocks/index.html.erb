<% content_for :title, "Stocks" %>

<div class="page-header">
  <h1>📦 Stocks</h1>
  <p class="description">登録されている在庫一覧です。</p>
</div>

<p style="color: green"><%= notice %></p>

<div class="actions">
  <%= link_to "➕ 在庫を追加する", new_stock_path, class: "btn btn-primary" %>
</div>

<hr>

<div id="stocks" class="stock-list">
  <% if @stocks.any? %>
    <% @stocks.each do |stock| %>
      <div class="stock-card">

        <h3><%= stock.name %></h3>

        <p class="stock-info">
          数量：<%= stock.quantity %><br>
          登録日：<%= l(stock.created_at, format: :short) %>
        </p>

        <div class="stock-links">
          <%= link_to "📄 詳細を見る", stock, class: "btn btn-outline" %>
        </div>

      </div>
    <% end %>
  <% else %>
    <p>在庫がありません。新しく追加してみましょう！</p>
  <% end %>
</div>

<hr>

<div class="footer-links">
  <%= link_to "🏠 トップに戻る", root_path %>
</div>
